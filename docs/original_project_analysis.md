# Original Project Record

이 문서는 2022년 원본 프로젝트 산출물 중 발표자료와 열교환기 설계 엑셀을 다시 읽고, 원본 모델의 핵심 가정, 입력값, 계산 흐름을 기록해 두는 보존용 메모다. 목적은 `원본이 실제로 무엇을 고정했고, 무엇을 열어 두었는지`를 안정적으로 남기는 것이다.

## Scope

- 대상은 원본 발표자료 1종과 열교환기 설계 엑셀 3종이다.
- 이 문서는 원본 산출물 해석에만 집중한다.
- 현재 재구현 모델이나 최신 출력은 여기서 관리하지 않는다.

## Reviewed Artifacts

- `D:\학교\3학년 2학기\열시스템디자인\프로젝트\A11.pptx`
- `D:\학교\3학년 2학기\열시스템디자인\프로젝트\열교환기 설계.xlsx`
- `D:\학교\3학년 2학기\열시스템디자인\프로젝트\열교환기 설계 - 1.xlsx`
- `D:\학교\3학년 2학기\열시스템디자인\프로젝트\열교환기 설계 (4구간).xlsx`

## Quick Take

- 원본은 `보조 가열기`를 명시적으로 모델링하지 않았다.
- 대신 LNG 기화기 hot-end 쪽 냉각유체 입구온도를 높은 값으로 먼저 두고, 그 온도에 맞는 유량과 열교환기 크기를 계산하는 방식이 강하다.
- 배관은 `10 km` 압력강하와 온도유지 문제로 분리되어 있지만, `배관을 거친 뒤 실제로 그 환수온도에 도달하는지`를 열수지로 닫은 흔적은 약하다.
- 따라서 이 문서는 원본 산출물이 실제로 어떤 가정 위에서 작성되었는지를 보존하는 기록 문서로 읽는 편이 맞다.

## Workbook Family

| File | Character | Notes |
| --- | --- | --- |
| `열교환기 설계.xlsx` | 메인 설계본 | `10 km 배관`, `15 MW`, `293 K` 환수온도 가정이 직접 보인다. |
| `열교환기 설계 - 1.xlsx` | 중간 실험본 | 시트 구조는 같고 일부 hot-end 온도 가정만 다르다. |
| `열교환기 설계 (4구간).xlsx` | 후속 4구간 버전 | LNG 상태량 표를 `293 K`까지 확장했고, 기화기 쪽 세분화가 더 진행돼 있다. |

## Reading Strategy

원본을 읽을 때는 아래 순서가 가장 자연스럽다.

1. `A11.pptx`로 전체 설계 흐름과 발표 논리를 본다.
2. `열교환기 설계.xlsx`로 메인 설계본의 입력값과 메모를 읽는다.
3. `열교환기 설계 - 1.xlsx`로 hot-end 온도 가정이 어떻게 흔들렸는지 확인한다.
4. `열교환기 설계 (4구간).xlsx`로 후속 세분화 방향을 확인한다.

## Key Cell Evidence

### 1. `열교환기 설계.xlsx`

#### `내부 열전달 계수 계산`

| Cell | Formula / Value | Interpretation |
| --- | --- | --- |
| `B7` | `15` | 총 열교환률을 `15 MW`로 직접 둔다. |
| `D4` | `283` | LNG 출구온도 `283 K`를 직접 둔다. |
| `H4` | `293` | 냉각유체 기화기 입구온도를 `293 K`로 직접 둔다. |
| `H18` | `220` | 냉각유체 기화기 출구온도를 `220 K`로 둔다. |
| `J14` | `=(J7*10^6)/(L8*(H4-H18))` | 위의 온도차를 이용해 냉각유체 유량을 계산한다. 계산값은 약 `89.98 kg/s`. |
| `S13` | `배관(10km) 압력강하` | 10 km 배관을 별도 검토 대상으로 둔다. |
| `S16` | `10km 배관 내경 [m]` | 배관 내경을 설계 변수로 둔다. |
| `R25` | `응축기에 도달할 때, 냉각유체의 온도는 몇 k이 적당할까?` | 환수온도는 별도 판단이 필요한 설계 입력으로 취급된다. |
| `R26` | `이걸 먼저 계산해서, 10km 배관 사이의 열교환량을 정의하자.` | 배관 열교환량을 정하자는 메모가 남아 있다. 완전히 닫힌 계산은 아직 아니다. |
| `L32:L38` | `240K R-290`, `250K r-717 (암모니아)`, `240K R-600a` | 후보 유체별 서로 다른 운전점을 따로 실험한 흔적이다. |

#### `X`

| Cell | Value | Interpretation |
| --- | --- | --- |
| `B40` | `143` | 냉각유체 온도 프로파일 표가 `143 K` 근처부터 시작된다. |
| `B41` | `144` | 배관 또는 냉각유체 축의 저온 시작점으로 보인다. |
| `B177` | `280` | 고온 영역이 `280 K`까지 계산된다. |
| `F4` | `='내부 열전달 계수 계산'!J14` | 이 시트는 별도 계산이 아니라 상위 시트에서 정한 유량을 참조한다. |
| `H4` | `='내부 열전달 계수 계산'!L8` | 냉각유체 비열 또는 참조값을 상위 시트에서 끌어온다. |

### 2. `열교환기 설계 - 1.xlsx`

이 파일은 메인 설계본과 구조가 거의 같지만 hot-end 온도 가정이 바뀌어 있다.

| Cell | Value | Interpretation |
| --- | --- | --- |
| `H4` | `295` | 기화기 입구 냉각유체 온도를 `295 K`로 높였다. |
| `H18` | `138` | 냉각유체 출구온도 가정이 메인 설계본과 크게 다르다. |
| `J14` | 약 `41.84 kg/s` | 같은 틀에서 운전점만 바꿔 본 중간 실험본에 가깝다. |

### 3. `열교환기 설계 (4구간).xlsx`

#### `내부 열전달 계수 계산`

| Cell | Value | Interpretation |
| --- | --- | --- |
| `B7` | `15` | 여전히 총 열교환률은 `15 MW`로 둔다. |
| `D4` | `283` | LNG 출구온도 `283 K` 유지 |
| `H4` | `330` | 기화기 입구 냉각유체 온도가 더 높은 값으로 고정된다. |
| `H18` | `160` | 냉각유체 출구온도도 다른 버전으로 변경됨 |
| `R14` | `=0.001*((O8*N14*P14^2)*R3)/(2*O14)` | 압력강하 계산식은 남아 있지만 |
| `S13:S16` | 비어 있음 | 메인 설계본의 `10 km` 배관 검토 구조가 빠져 있다. |

#### `LNG 상태량`

| Cell | Value | Interpretation |
| --- | --- | --- |
| `A171` | `280` | 고온 상태량 표가 `280 K`까지 이어진다. |
| `A177` | `286` | 상태량 표를 `286 K`까지 확장한다. |
| `A184` | `293` | 상태량 표를 `293 K`까지 확장한다. |
| `B178` | `54.143` | 해당 온도에서의 물성/엔탈피 관련 값 |
| `D178` | `0.039144` | 상태량 보조값 |

이 버전은 `기화기 쪽 계산을 4구간으로 세분화`하려는 의도가 분명하지만, 환수온도를 계산으로 닫기보다는 `286~293 K` 영역을 유체 상태표에 포함시켜 두는 방식이다.

## Shared Signals Across The Excel Files

세 파일을 같이 읽으면 공통 신호가 보인다.

1. LNG 출구온도 `283 K`는 거의 고정된 목표값이다.
2. 총 기화 duty는 `15 MW` 수준의 고정 입력으로 시작한다.
3. 기화기 입구 냉각유체 온도는 계산 결과라기보다 우선 입력되는 값에 가깝다.
4. 배관은 중요한 검토 대상이지만, 열적 closure보다 압력강하 검토 흔적이 더 분명하다.
5. 후보 냉각유체는 통합 최적화보다 개별 운전점 비교 방식으로 다뤄진다.

## PPT Evidence

### `A11.pptx`

#### Slide 35

- `냉각유체의 냉열과 냉각수 사이의 열교환율: 14 MW`
- `응축기 출구 온도 < 냉각수 입구 온도 (285 K)`

원본 발표는 배관 설계 파트에서 이미 특정 응축기/환수 조건을 설계 입력처럼 제시한다.

#### Slide 36

- `냉각유체가 10 km를 이동하는 동안 0.4 MPa 이상이어야 함`
- `응축기 출구 온도를 280 K로 설정`
- `응축기 입구 온도 143~144 K을 위한 배관 설계`
- `IDC → 기화기 배관: 지열과의 열교환이 이루어지므로 직경이 커도 가능`

이 슬라이드는 원본 모델이 `배관에서 열을 받아 환수 라인이 상당히 따뜻해진다`고 보았음을 보여준다. 다만 그 열량이 정확히 얼마나 되는지, 그리고 그 열이 LNG hot-end 요구온도를 자동으로 만족시키는지는 발표 슬라이드만으로 닫혀 있지 않다.

#### Slide 38

- 공급/환수 배관 압력강하 합계 `0.599 MPa`
- `부차손실`은 별도 메모 수준으로 남음

즉 배관 설계의 중심도 `압력강하`이며, 열적 closure는 압력강하만큼 엄밀하게 정리되지 않은 것으로 보인다.

## Slide Flow Snapshot

`A11.pptx`의 큰 흐름은 아래처럼 읽힌다.

1. 예비설계와 기존 R-134a 기준 사이클
2. 냉각유체 후보 선정
3. LNG 기화기 설계
4. 순환 배관 설계
5. 열역학 평가
6. 추가 고려사항과 정리

즉 발표는 `배경 설명`보다 `설계 절차`를 앞세우는 구성이다.

## Original Model Assumptions

원본 프로젝트는 아래 가정을 암묵적으로 사용한 것으로 해석된다.

1. 기화기 hot-end 냉각유체 입구온도는 설계 입력으로 둔다.
2. 총 기화 duty는 `15 MW` 수준의 고정값으로 시작한다.
3. `10 km` 배관은 압력강하와 열유입의 trade-off 대상이지만, 환수온도는 먼저 정하거나 범위를 가정한다.
4. `IDC → 기화기` 구간의 열 획득은 `지열` 또는 주변열과의 열교환으로 충분히 설명될 수 있다고 본다.
5. 냉각유체 비교는 통합 최적화보다는 `유체별 별도 운전점`을 비교하는 방식이다.

## Original Model Characteristics

원본 산출물만 놓고 보면, 모델의 특징은 아래처럼 정리된다.

1. 기화기 hot-end 냉각유체 입구온도는 설계의 핵심 입력값이다.
2. 총 기화 duty는 `15 MW` 수준의 고정값으로 시작한다.
3. `10 km` 배관은 압력강하와 온도유지를 같이 검토하지만, 배관 열수지는 완전히 닫혀 있지 않다.
4. `IDC → 기화기` 구간은 주변열 또는 `지열과의 열교환`으로 충분한 열을 얻는 것으로 해석된다.
5. 냉각유체 비교는 통합 최적화보다 `유체별 후보안 비교`에 가깝다.
6. `(4구간)` 버전은 메인 설계본보다 hot-end 구간을 더 세분화하려는 후속 작업으로 보인다.

## What The Original Explicitly Fixes

원본에서 직접 고정된 값으로 읽히는 항목은 다음과 같다.

- LNG 출구온도 `283 K`
- 총 열교환률 `15 MW`
- 기화기 입구 냉각유체 온도 `293 K`, `295 K`, `330 K` 등 버전별 가정값
- 10 km 배관이라는 거리 조건
- `143~144 K` 수준의 저온 구간
- `280 K` 응축기 출구 조건

즉 원본은 먼저 중요한 온도와 열량을 정하고, 그 뒤에 유량, 압력강하, 열교환기 길이, 배관 직경을 맞추는 흐름이다.

## What The Original Leaves Open

반대로 원본에서 명확히 닫히지 않은 항목도 있다.

- 기화기 입구 냉각유체 온도에 실제로 어떻게 도달하는가
- `IDC → 기화기` 구간에서 얻는 열량이 정확히 얼마인가
- `지열과의 열교환`이 실제 설계 입력인지, 개념 설명인지
- 10 km 배관의 열유입과 환수온도 상승이 기화기 hot-end 요구와 일관되게 연결되는가
- `(4구간)` 버전의 `330 K` 가정이 최종안인지, 중간 실험값인지

즉 원본은 `완전히 닫힌 열수지 모델`이라기보다, 설계 판단과 계산 시트가 함께 섞인 엔지니어링 작업본에 가깝다.

## Original Reading Notes

- `열교환기 설계.xlsx`는 메인 설계본이다.
- `열교환기 설계 - 1.xlsx`는 같은 형식의 변형본 또는 중간 실험본으로 보인다.
- `열교환기 설계 (4구간).xlsx`는 hot-end 및 상태량 범위를 확장한 후속 버전으로 보인다.
- `A11.pptx`는 최종 제출용 발표자료이며, 계산의 세부 유도보다 설계 논리와 결과 전달에 초점이 있다.

## Recommended Use Of This Note

이 문서는 아래 용도로 쓰는 편이 좋다.

1. 원본 발표와 엑셀의 해석 기준점 보존
2. 원본 엑셀의 셀 근거를 빠르게 다시 찾기
3. 원본이 암묵적으로 둔 경계조건을 추적하기
4. 나중에 별도 비교 문서를 만들 때 원본 쪽 사실 관계를 고정하기

## Open Questions Inside The Original

- `293 K`와 `295 K`는 최종 목표 환수온도였는가, 아니면 임시 입력이었는가
- `330 K`는 오타인지, 다른 경계조건을 둔 실험값인지
- `지열과의 열교환`은 실제 모델 일부였는가, 아니면 개념 설명 수준이었는가
- 세 엑셀 파일 중 어느 버전이 시간적으로 최종본에 가장 가까운가

이 질문들은 원본 기록을 더 읽어야만 답할 수 있는 항목들이다. 따라서 이 문서는 원본을 해석하는 작업 노트로 유지하고, 이후 모델 비교가 필요할 때만 별도 문서에서 다루는 편이 적절하다.
